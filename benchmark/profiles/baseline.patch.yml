# Baseline group: the official repeat-tool-reminder only (dsh-base default).
# This overlay is intentionally empty; it exists so every group passes a
# --patch file and the runner treats them symmetrically.
