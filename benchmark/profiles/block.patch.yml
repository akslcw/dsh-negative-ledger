# Block group: official reminder off; Negative Ledger block over sqlite.
# __LEDGER_DIR__ is replaced per run by benchmark/run.ts.
- id: repeat-tool-reminder
  disabled: true

- insert:
    - id: negative-ledger
      name: ../../../../src/plugin.ts
      config:
        backend: sqlite
        mode: block
        dir: __LEDGER_DIR__
